# This is the spring-boot backend service of QuickPharma app written in React.

# Steps to run
1. First install MySql in local and change the resources/application.property accordingly.
2. Run the app as spring boot. 